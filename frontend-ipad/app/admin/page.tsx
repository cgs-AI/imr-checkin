export default function AdminLanding() {
  return (
    <section className="card">
      <h1>Admin</h1>
      <p className="muted">
        Admin tooling requires authentication. The MVP exposes admin endpoints
        on the backend; a dedicated UI will be added in Phase 4.
      </p>
    </section>
  );
}
